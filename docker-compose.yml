name: memoo

services:
  postgres:
    image: postgres:16
    container_name: memoo-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - '${POSTGRES_PORT:-5432}:5432'
    volumes:
      - memoo_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}']
      interval: 5s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    container_name: memoo-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    ports:
      - '${MINIO_PORT:-9000}:9000'
      - '${MINIO_CONSOLE_PORT:-9001}:9001'
    volumes:
      - memoo_minio_data:/data
    healthcheck:
      test: ['CMD', 'mc', 'ready', 'local']
      interval: 5s
      timeout: 5s
      retries: 5

  minio-init:
    image: minio/mc:latest
    container_name: memoo-minio-init
    depends_on:
      minio:
        condition: service_healthy
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      MINIO_BUCKET: ${MINIO_BUCKET}
    entrypoint: >
      /bin/sh -c "
      mc alias set memoo http://minio:9000 $${MINIO_ROOT_USER} $${MINIO_ROOT_PASSWORD} &&
      mc mb --ignore-existing memoo/$${MINIO_BUCKET} &&
      mc anonymous set download memoo/$${MINIO_BUCKET} &&
      exit 0;
      "

  adminer:
    image: adminer:latest
    container_name: memoo-adminer
    restart: unless-stopped
    ports:
      - '${ADMINER_PORT:-8080}:8080'
    depends_on:
      postgres:
        condition: service_healthy

  api:
    build:
      context: ./apps/api
      dockerfile: Dockerfile
    container_name: memoo-api
    restart: unless-stopped
    ports:
      - '${API_PORT:-8000}:8000'
    # Bind-mount source so edits on the host trigger uvicorn --reload
    volumes:
      - ./apps/api:/app
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ROOT_USER}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD}
      MINIO_BUCKET: ${MINIO_BUCKET}
      MINIO_USE_SSL: ${MINIO_USE_SSL:-false}
      MINIO_PUBLIC_URL: ${MINIO_PUBLIC_URL}
      CORS_ORIGINS: ${CORS_ORIGINS}
      APP_ENV: ${APP_ENV:-development}
      APP_DEBUG: ${APP_DEBUG:-true}
      GOOGLE_API_KEY: ${GOOGLE_API_KEY:-}
      GEMINI_MODEL: ${GEMINI_MODEL:-gemini-2.5-flash}
      SANDBOX_CDP_URL: http://sandbox:9223
      STAGEHAND_SERVICE_URL: http://agent:8787
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
      minio-init:
        condition: service_completed_successfully
      sandbox:
        condition: service_healthy
      agent:
        condition: service_healthy

  agent:
    build:
      context: ./apps/agent
      dockerfile: Dockerfile
    container_name: memoo-agent
    restart: unless-stopped
    volumes:
      - ./apps/agent:/app
      - /app/node_modules
    environment:
      PORT: 8787
      GOOGLE_API_KEY: ${GOOGLE_API_KEY:-}
      GOOGLE_GENERATIVE_AI_API_KEY: ${GOOGLE_API_KEY:-}
      GEMINI_MODEL: ${GEMINI_MODEL:-gemini-2.5-flash}
      STAGEHAND_MODEL: ${GEMINI_MODEL:-gemini-2.5-flash}
    depends_on:
      sandbox:
        condition: service_healthy
    healthcheck:
      test:
        [
          'CMD',
          'node',
          '-e',
          "fetch('http://localhost:8787/health').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 5s
      timeout: 5s
      retries: 10

  sandbox:
    build:
      context: ./apps/sandbox
      dockerfile: Dockerfile
    container_name: memoo-sandbox
    restart: unless-stopped
    ports:
      - '${SANDBOX_NOVNC_PORT:-6080}:6080'
      - '${SANDBOX_CDP_PORT:-9222}:9223'
    environment:
      RESOLUTION: '1280x800x24'
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:8585/health']
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

  web:
    build:
      context: ./apps/web
      dockerfile: Dockerfile
    container_name: memoo-web
    restart: unless-stopped
    ports:
      - '${WEB_PORT:-3000}:3000'
    # Bind-mount source for hot-reload; anonymous volumes shadow
    # node_modules and .next so container copy is used, not host's.
    volumes:
      - ./apps/web:/app
      - /app/node_modules
      - /app/.next
    environment:
      NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL}
      NEXT_PUBLIC_SANDBOX_NOVNC_URL: http://localhost:${SANDBOX_NOVNC_PORT:-6080}/vnc.html?autoconnect=true&resize=scale
      NEXT_PUBLIC_GEMINI_API_KEY: ${NEXT_PUBLIC_GEMINI_API_KEY:-}
      NEXT_PUBLIC_GEMINI_LIVE_MODEL: ${NEXT_PUBLIC_GEMINI_LIVE_MODEL:-gemini-2.5-flash-native-audio-preview-12-2025}
      API_BASE_URL_INTERNAL: http://api:8000/api
    depends_on:
      - api

volumes:
  memoo_pg_data:
  memoo_minio_data:
